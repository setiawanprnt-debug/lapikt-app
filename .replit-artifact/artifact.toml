kind = "web"
previewPath = "/"
title = "LapIKT"
version = "1.0.0"
id = "artifacts/lapikt"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19382

[services.development]
run = "pnpm --filter @workspace/lapikt run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lapikt", "run", "build" ]
publicDir = "artifacts/lapikt/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19382"
BASE_PATH = "/"
